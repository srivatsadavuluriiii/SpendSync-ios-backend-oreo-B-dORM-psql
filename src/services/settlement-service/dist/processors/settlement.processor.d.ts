/**
 * Process a settlement job
 * @param {Object} job - Bull job object
 * @returns {Promise<Object>} - Result of the settlement processing
 */
export function processSettlementJob(job: Object): Promise<Object>;
