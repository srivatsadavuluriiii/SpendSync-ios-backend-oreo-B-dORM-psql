/**
 * Process a settlement reminder job
 * @param {Object} job - Bull job object
 * @returns {Promise<Object>} - Result of the reminder processing
 */
export function processSettlementReminder(job: Object): Promise<Object>;
